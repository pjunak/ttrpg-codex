{
  "id": "demo-contrib",
  "name": "Contribution demo",
  "version": "0.1.0",
  "apiVersion": 1,
  "hostVersion": ">=1.0.0",
  "entry": "entry.js",
  "permissions": ["ui:slot:timeline", "kinds:connections", "kinds:graph", "graph:contribute"],
  "tests": { "client": "tests/demo-contrib.addon-test.mjs" },
  "summary": "Demonstrates every data-driven contribution seam: timeline session-card + column content slots (ui:slot:timeline), a custom mind-map connection kind (kinds:connections), and a custom mind-map NODE kind injected into the relationships view (kinds:graph + graph:contribute). A template for injecting addon content/kinds INTO existing core surfaces."
}
