const noop = () => {};

export const ADDON_PERMISSION_CASES = Object.freeze([
  { method: 'registerRoute', permission: 'ui:route',
    invoke: host => host.registerRoute('permission-probe', noop) },
  { method: 'registerSidebarPage', permission: 'ui:sidebar',
    invoke: host => host.registerSidebarPage({ route: 'permission-probe', label: 'Probe' }) },
  { method: 'registerPageRenderer', permission: 'ui:route',
    invoke: host => host.registerPageRenderer('permission-probe', noop) },
  { method: 'registerArticleSection', permission: 'ui:article-section:characters',
    invoke: host => host.registerArticleSection('characters', noop) },
  { method: 'registerSettingsTab', permission: 'ui:settings-tab',
    invoke: host => host.registerSettingsTab({ id: 'permission-probe', render: noop }) },
  { method: 'registerAction', permission: 'ui:action',
    invoke: host => host.registerAction('permission-probe', noop) },
  { method: 'registerCollection', permission: 'data:own',
    meta: { collections: [{ name: 'permission_probe', keyed: false, access: 'public' }] },
    invoke: host => host.registerCollection('permission_probe') },
  { method: 'registerWikiKind', permission: 'wiki:kind',
    invoke: host => host.registerWikiKind('permission-probe', noop) },
  { method: 'registerEditorFields', permission: 'ui:editor-fields:characters',
    invoke: host => host.registerEditorFields('characters', { fields: noop }) },
  { method: 'registerFragmentOp', permission: 'ui:override',
    invoke: host => host.registerFragmentOp('characters:permission-probe', { op: 'hide' }) },
  { method: 'registerSlot', permission: 'ui:slot:dm',
    invoke: host => host.registerSlot('dm:permission-probe', noop) },
  { method: 'registerKind', permission: 'kinds:statuses',
    invoke: host => host.registerKind('statuses', { id: 'permission-probe', label: 'Probe' }) },
  { method: 'registerConnectionKind', permission: 'kinds:connections',
    invoke: host => host.registerConnectionKind({ id: 'permission-probe', label: 'Probe' }) },
  { method: 'registerNodeKind', permission: 'kinds:graph',
    invoke: host => host.registerNodeKind({ id: 'permission-probe', cardHTML: noop }) },
  { method: 'registerGraphView', permission: 'kinds:graph',
    invoke: host => host.registerGraphView({ id: 'permission-probe', label: 'Probe', build: noop }) },
  { method: 'registerGraphContributor', permission: 'graph:contribute',
    invoke: host => host.registerGraphContributor('permission-probe', noop) },
  { method: 'store.getCharacters', permission: 'data:read:characters',
    invoke: host => host.store.getCharacters() },
  { method: 'store.getLocations', permission: 'data:read:locations',
    invoke: host => host.store.getLocations() },
  { method: 'store.getEvents', permission: 'data:read:events',
    invoke: host => host.store.getEvents() },
  { method: 'store.getMysteries', permission: 'data:read:mysteries',
    invoke: host => host.store.getMysteries() },
  { method: 'store.getFactions', permission: 'data:read:factions',
    invoke: host => host.store.getFactions() },
  { method: 'store.getCollection', permission: 'data:read:pantheon',
    invoke: host => host.store.getCollection('pantheon') },
  { method: 'store.patchAddonData', permission: 'data:write:characters.addonData',
    invoke: host => host.store.patchAddonData('characters', 'permission-probe', () => ({})) },
]);
