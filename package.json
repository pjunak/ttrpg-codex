{
  "name": "ttrpg-codex",
  "version": "2.0.0-dev",
  "description": "Self-hostable web codex for D&D and other TTRPGs",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "go run ./cmd/codex",
    "build": "npm --workspace @ttrpg-codex/frontend run build && go build ./cmd/codex",
    "test": "npm --workspace @ttrpg-codex/frontend test && go test ./cmd/... ./contracts/... ./internal/... ./sdk/...",
    "typecheck": "npm --workspace @ttrpg-codex/frontend run typecheck",
    "check": "npm --workspace @ttrpg-codex/frontend run check && go test ./cmd/... ./contracts/... ./internal/... ./sdk/... && go vet ./cmd/... ./contracts/... ./internal/... ./sdk/...",
    "release-check": "node scripts/check-release-readiness.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
