{
  "name": "@ttrpg-codex/frontend",
  "version": "2.0.0-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:browser": "node --test test/browser/*.browser.mjs",
    "check": "npm run typecheck && npm test && npm run test:browser && npm run build"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "lit": "3.3.3",
    "marked": "18.0.11"
  },
  "devDependencies": {
    "playwright": "1.62.0",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
